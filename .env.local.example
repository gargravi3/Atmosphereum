# Render Postgres external connection string.
# Go to Render dashboard -> Postgres -> External Connection String (ends in ?sslmode=require or similar).
# Keep DATABASE_URL secret; .env.local is gitignored.
DATABASE_URL=postgresql://user:pass@hostname.oregon-postgres.render.com:5432/atmosphereum?sslmode=require
